# Prompt 5: Object Mappers

Context:
You are a senior developer. We need to map between Domain Entities and Infrastructure (Dapper) Models.
Constraints:
- Manual mapping (no external libraries like AutoMapper).
- Use extension methods for mapping (e.g., .ToDomain(), .ToModel()).
Output:
Mapping classes with extension methods for bidirectional mapping where necessary.
